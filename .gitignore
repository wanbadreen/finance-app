node_modules/
dist/
.env
.env.local
# Local credentials and environment variants
.env.*
!.env.example

# Database dumps, receipts, and backup manifests
Kira-Backups/

# Android signing material must never enter source control
*.jks
*.keystore
key.properties
keystore.properties
